import styled from "styled-components";

export default () => {
  return (
    <HeaderWrapper className="flex_row">
      Buck.Bug.Blog
    </HeaderWrapper>
  )
};

const HeaderWrapper = styled.header`
  height: 60px;
`;